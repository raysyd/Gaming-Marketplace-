/** One copy, shared by every state <select> (account settings, onboarding, sell form, shop filter) instead of four copies drifting apart. */
export const AU_STATES = ["NSW", "VIC", "QLD", "WA", "SA", "TAS", "ACT", "NT"];
